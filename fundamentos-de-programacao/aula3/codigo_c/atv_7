#include <stdio.h>

int main() {
    float num;

    printf("Digite um número: ");
    scanf("%f", &num);

    if (num == (int)num) {
        printf("O número %.2f é inteiro.\n", num);
    } else {
        printf("O número %.2f é quebrado (decimal).\n", num);
    }

    return 0;
}
